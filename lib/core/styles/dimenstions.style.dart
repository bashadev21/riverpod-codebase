import 'package:flutter/material.dart';

class Dimen {
  static const margin = 16.0;
  static const halfMargin = 8.0;
  static const space = 8.0;
  static const icon = 16.0;
  static const alertContent = 24.0;
  static const sigma = 8.0;

  static const scaffoldMargin = EdgeInsets.all(margin);
  static const textMargin = EdgeInsets.all(margin / 2);

  static const horizontalMargin = EdgeInsets.symmetric(horizontal: margin);
  static const verticalMargin = EdgeInsets.symmetric(vertical: margin);
}
